<li>
	{{burgername}}

	<button class="change-devour" data-id="{{id}}" data-neweat="{{devour}}">
		{{#if devour}} Devour it! {{else}} Later {{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete</button>
</li>
